{% extends "base.html.twig" %}
{% block title %}Update Password{% endblock %}
{% block body %}
    <div class="container passwordAp bg-light py-3 px-3">
        <h1>Update password</h1>
        {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit" class="btn btn-secondary">Change password</button>
        {{ form_end(form) }}
    </div>
{% endblock %}