{% extends "base.html.twig" %}
{% block title %}Edit profile user{% endblock %}
{% block body %}
    <div class="profile container bg-light py-3 px-3">
        <h1>Edit profil</h1>
         {{ form_start(form) }}
        {{ form_widget(form) }}

        <button type="submit" class="btn btn-primary">Save the changes</button>
        {{ form_end(form) }}
    </div>
   
{% endblock %}